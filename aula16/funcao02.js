function soma(n1=0, n2=0) {
    return n1 + n2
}

console.log(soma(2))

//se não definir o n1 como recebendo um valor, quando tetarmos somar faltando um valor, o JS não ira reconhecer e colocará como resposta NaN (not a number). Por isso colocar o valor como recebe 0, cajo não haja um segundo número, ele ira mostar um resultado

